const numbers = [2, 5, 7, 8, 11, 21, 13];
// const square = [];

// for (let i = 0; i < numbers.length; i++) {
//     const element = numbers[i];
//     const result = element * element;
//     square.push(result);
// }

// const result = numbers.map(function(element, index, array){     //A map function inputs 3 parameters element, index of the element and array;
//     // return element * element;
//     console.log(element, index, array)
// })

// const result = numbers.map(x => x * x);



// const result = numbers.filter(x => x > 10);  //output: [11, 21, 13]      //filter returns an array consists of selected elements;
// const result = numbers.find(x => x > 10);    //output: 11        //find returns only one selected element not array;

// console.log(result)